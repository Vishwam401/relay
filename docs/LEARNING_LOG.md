# LEARNING_LOG.md — Master Index

> [!IMPORTANT]
> **This is an index only.** Detailed daily logs — measurements, code evidence,
> self-checks, and unresolved items — live in one file per week under `logs/`.

---

## 📁 Where things live

| Folder | Holds | Written |
|---|---|---|
| `planning/WEEK_NN.md` | The **plan** — what to do, experiments, checklists | Once, before the week starts |
| `logs/WEEK_NN.md` | The **log** — what happened, measured numbers, self-checks | Daily, during the week |
| `roadmap/CURRENT_WEEK.md` | Pointer to the active week | On week change |

**Plan and log are never the same file.** The plan states intent, the log states outcome.
Keeping them apart is what makes *"did the original goal get met?"* answerable later.

---

## 📚 Logs by Week

| Week | Title / Focus | Plan | Log | Status |
|---|---|---|---|---|
| **Week 0** | Systems & Concurrency Foundations (Days 1–5) | [plan](planning/WEEK_00.md) | [**log**](logs/WEEK_00.md) | ✅ Complete |
| **Week 1** | Job Engine & DB-backed Queue (`SKIP LOCKED`) | [plan](planning/WEEK_01.md) | [**log**](logs/WEEK_01.md) | 🔄 Din 1–6 done · **Din 7 ran and was measured, but has no log entry** |
| **Week 2** | Lease, heartbeat, reaper — atka hua job wapas kaun laayega | [plan](planning/WEEK_02.md) | [**log**](logs/WEEK_02.md) | ✅ **Complete — Din 1 `partial` · Din 2 `partial` · Din 3 `yes` · Din 4 `partial` · Din 5 `partial` · Din 6 (close) `yes`.** Shipped: lease (`claimed_at`), reaper, heartbeat, bounded retry with backoff + equal jitter, `dead_letter` via `NOT VALID` + `VALIDATE`. Measured: `14.783 s` of real overlap on job 95, reclaim latency `1.798192 s`, the `ACCESS EXCLUSIVE` queue, `attempts = 4` above the bound. Din 6 closed the five-day chain **exactly** across five status buckets (`107` rows, `94` execution rows), wrote `D-22`/`D-23` and amended `D-06`/`D-21`. **Contract #2 is still unprotected — the duplicate window narrowed, it did not close.** Handoff: [`daily/WEEK_02_HANDOFF.md`](daily/WEEK_02_HANDOFF.md) |
| **Week 3** | Idempotency & Deduplication Engine | [plan](planning/WEEK_03.md) | [**log**](logs/WEEK_03.md) | ✅ **Complete (`2026-09-05`) — Din 1–6 complete.** Five-day chain reconciled on DB date groups (`119` jobs, `107` executions, `9` effects; buckets `97/15/4/3/0`). Published `D-24` (two-layer idempotency: enqueue vs execute), audited `D-25`, and appended amendments for `D-03`, `D-05`, `D-21`, `D-22`. Din 5 property testing established Layer A model safety within finite examples; Layer B production-worker witness, fencing token, `completed_at`, and 45 s shutdown run slipped to Week 4; outbox dispatcher build deliberately deferred to Week 4. Final score: `6.5/10.0`. Next free: `D-26`, `P-30`. Handoff: [`daily/WEEK_03_HANDOFF.md`](daily/WEEK_03_HANDOFF.md). |
| **Week 4** | Fencing, outbox, load & production writeup | [plan](planning/WEEK_04.md) | [**log**](logs/WEEK_04.md) | ✅ **Complete (`2026-09-11`) — Din 1–6 done. MONTH 1 CLOSED.** **Din 6:** `src/` untouched (tree SHA `394338c…` identical at C0 and C6). Published `D-26` (fencing token, only the claim CAS increments it), `D-27` (transactional outbox; intent atomic, delivery at-least-once, exactly-once is the **receiver's** job), `D-28` (four SQL metrics + `/healthz` liveness-only, and the four things it deliberately does **not** report), `D-29` (no leader election — and the advisory lock is rejected **on scope, not on measurement**, because the two-reaper double-reclaim count was never taken). Amendment blocks appended under `D-06`, `D-21`, `D-22`, `D-23`, `D-25`. Four-week reconcile chain joined **line by line**, including `sink_deliveries`' `−3` (`P-46`) — and the three-arm differential is what proves it: `count(*) = 7`, `count(distinct key) = 7`, `sink_deliveries_id_seq.last_value = 10`. **`count(*)` lies after a `DELETE`; the sequence does not.** Full `README.md` rewrite: nine-row failure matrix, **zero empty cells**, three `[NO EVIDENCE]` rows, eight transaction boundaries drawn, zero banned vocabulary. **Month 1 verdict — not one of the five promises is a bare `protected`:** #1 `narrowed` (scope is `202`'s semantics — a request arriving while Postgres is down is **rejected, not accepted**), #2 `narrowed` **with different verdicts for local and external** (external is Relay's *requirement on the receiver*, not its guarantee), #3 `narrowed` (bound is on **scheduling**, not dispatches — `P-27`; and `os._exit` skips its branch — `P-36`), #4 `narrowed` at the **row** level and **`[NO EVIDENCE]`** at the **process** level (`P-43`), #5 `narrowed` (strongest, and reachable only through the handler-exception path). Blog post **`deliberately deferred`** to Month 2 Week 1 with a named owner, per the day's own cut order — evidence over publishing. Two corrections measured today: `dead_letter` ids are `104,105,108,129,130` (the day's own Step 1 entry listed three wrong ids while the count was right — exactly the failure per-line reconciliation exists to catch), and `job_executions` `NULL` generations number **`113`**, not the `107` the brief carried. Frozen score `0.0/5.0` — **all five `idk`, and all five were answerable from `P-40`/`P-42`/`P-43`/`P-45`/`P-46`, written in the previous 48 hours. A reading gap, not a knowledge gap.** Week frozen total `3.75/31.0`. Grade `8.5/10`. Next free: **`D-30`**, **`P-47`**. Handoff: [`daily/WEEK_04_HANDOFF.md`](daily/WEEK_04_HANDOFF.md). **Month 2's first item is `P-43`, and it is not a feature.** <br><br> <details><summary>Din 1–5 detail</summary> **Din 1–5 done.** **Din 1:** Generation fencing token implemented (`claim_generation`) and verified. **Din 2 (`2026-09-07`):** `completed_at` + `last_error` migration and gated writes, `signal_to_handler = 42.068s` on blocking handler. **Din 3 (`2026-09-08`):** Transactional outbox migration (`w4d3_outbox`), atomic co-commit with `side_effects` (`xmin_same|true`), receiver service (`src/sink.py`), outbox dispatcher (`src/dispatcher.py`) with `FOR UPDATE SKIP LOCKED` across HTTP call. **Din 4 (`2026-09-09`):** Receiver `UNIQUE (idempotency_key)` and atomic `ON CONFLICT DO NOTHING`, real multi-process interleavings A, B, B′, C, D. **Din 5 (`2026-09-10`):** Carried repairs `P-38` (dual sink unique) and `P-39` (alembic `-c` precedence) — both live and independently verified: `alembic current` now announces `source=alembic.ini`, and `src/sink.py`'s DDL no longer carries an inline `UNIQUE`. Connection budget measured (`100` max, `3` reserved, `5` idle, `75` fleet ceiling; **headroom `~3-5`, not `22`** — the log's figure omitted the load script's engine and `psql` sessions). `application_name` attribution working across all five processes, which is the day's cleanest gate. Sustained load: `400` jobs at `19.8`/s arrival against a drain rate of **`7.01 jobs/s`** / `0.1427 s` per job, recomputed independently from the log's own timestamps; `186` processed `+` `214` pending `=` `400`; `POLL_INTERVAL_SECONDS` never entered the arithmetic. Four metrics captured (p50 `10.2849 s`, p99 `18.3357 s`, retry `0.0%`, DLQ `0`). **Two headline conclusions were refuted at review by the day's own logs:** the worker did **not** survive the Postgres outage — it exited through `asyncio.run` because `except Exception` wraps only handler execution and the claim poll is unguarded — and the reaper's reclaim happened only because the reaper was **launched by hand after** recovery, so `7.9 s` is process-launch latency rather than a recovery bound (`P-43`). Only one of the two Postgres-down failures was actually observed (stale-pool, not live-break), and the `MAX_ATTEMPTS` question remains unmeasured because no job was in a handler at the time. Steps 2 and 3 retained **no** artifacts, so three headline numbers are `[REPORTED, NOT VERIFIABLE]` (`P-45`), and `/slow-hold` shipped into `src/main.py` unauthenticated and unbounded (`P-44`). Evidence DB delta `0` on the eight asserted counters (`133|145|19|4|39|5|0|1`), Job 136 untouched, migrated to HEAD (`w4d4_sink_unique`), `alembic check` green, `0` probe DBs, `0` processes. Quiz score corrected `1.5 → **1.0/5.0**`. Grade `7.5/10`. Next free: `D-26`, `P-46`. </details> |


---

## 🔗 Companion documents

| File | Contents |
|---|---|
| [`DECISIONS.md`](DECISIONS.md) | Architecture decisions, **29 entries at Month 1 close**. `D-01`/`D-02` = queue/claim · `D-03`..`D-08` = `jobs` schema · `D-21` = `job_executions` · `D-22`/`D-23` = lease/retry · `D-24`/`D-25` = two-layer idempotency and local-effect identity. **Week 4 Din 6 published `D-26`** (`jobs.claim_generation` fencing token, incremented **only** by the claim CAS), **`D-27`** (transactional outbox — intent atomic, delivery at-least-once, exactly-once is the receiver's job), **`D-28`** (four SQL metrics + `/healthz` liveness-only, plus the endpoint inventory and the `pool_pre_ping` rejection **re-taken** because Din 5's premise was false — `P-43`), and **`D-29`** (no leader election; advisory lock rejected **on scope, not on measurement**). Amendment blocks: `D-03`, `D-05` (Week 3 Din 6) · `D-21`, `D-22` (Week 3 Din 6 **and** Week 4 Din 6) · `D-06`, `D-23`, `D-25` (Week 4 Din 6). **Next free: `D-30`.** `D-09`..`D-20` belong to roadmap Part 2. |
| [`PROBLEMS.md`](PROBLEMS.md) | Problem explorations, **`P-01`..`P-47` at Month 1 close. Next free `P-48`.** **`P-47`** was opened on Din 6 at commit time: five `docs/` subtrees are gitignored, so all four handoffs, every BRIEF/KEY, and all twelve `PREDICTIONS_FROZEN.md` files are in no commit — and the frozen seal is verified by hashing a file the repository does not contain. Week 4 Din 5's review added the four that Month 1's verdicts rest on: **`P-43`** (no exception boundary on the worker/reaper/dispatcher poll, and no supervisor anywhere — a database restart kills the process, which is why promise #4 is `[NO EVIDENCE]` at the process level), **`P-44`** (`/slow-hold` shipped into `src/main.py`: unauthenticated, unbounded, and the amplifier for the very cascade it was written to measure), **`P-45`** (three headline numbers with no retained artifact, plus `requirements.txt` pinning nothing), and **`P-46`** (a migration ran an unguarded `DELETE` on the evidence database, destroying the raw evidence for `P-33` and `P-40` — and the day's delta gate was structurally unable to see it, because the frozen set excluded the exact bucket where the change landed). *Historical note, superseded:* this row previously read `P-01..P-32`, next free `P-33`. <br><br> <details><summary>Earlier detail (Week 3 / Week 4 Din 2–4)</summary> Week 4 Din 2 added **`P-30`** (a rejected mark withholds `last_error` too, so the only worker that saw the exception leaves no reason — `P-25`'s third carrier), **`P-31`** (the closing bench reconciles totals but never asserts the queue is drained, so Din 2 closed with three carried `pending` rows and printed pass), and **`P-32`** (the lifecycle-grep gate read only the worker's log, so the reaper's reclaim line stayed un-greppable for two days — fixed in `src/reaper.py`, gate gap still open) `[MEASURED-R]`. Din 4 amended **`P-07`**: current opt-in local enqueue retries are resolved by caller key + fingerprint + database arbitration, while omitted/lost keys, row deletion, application-owned key/fingerprint pairing, and future caller scope remain explicit boundaries `[MEASURED-R/INFERRED]`. Week 3 Din 3 review added **`P-29`**: final rows cannot prove temporary crash-hook placement or that negative-control processes were alive; fresh reproduction validates the mechanism but does not replace the missing original transcript `[MEASURED-R/INFERRED]`. `P-28` remains open: runtime honors `DATABASE_URL` while Alembic uses the fixed `alembic.ini` URL, so every disposable migration must explicitly override and verify `current_database()` `[MEASURED-R]`. </details> |
| [`ddia_summaries/DDIA_CH8_LINKS.md`](ddia_summaries/DDIA_CH8_LINKS.md) | Ch 8 → Relay mappings, **18 lines, all present** — `1`–`13` and `17`–`18` are Ch 8 links, `14`–`16` are Din 4's Marc Brooker links `[MEASURED 2026-09-11]`. **`DDIA_CH9_LINKS.md` does not exist** and is `slipped` to Month 2; it is also the reading that would price `D-29`'s advisory-lock alternative. Lines 10–13 are **reviewer-written** at Din 3's close after three consecutive append slips — the user must confirm them in his own words. Lines 14–16 (Din 4) and 17–18 (Din 5) are the **user's own**, written on the day they were owed; three of those five carry a reviewer note beneath them correcting a parenthetical, and line 18's mechanism is `[INFERRED]` because Din 5's run had handler `<` lease |
| [`POSTMORTEMS.md`](POSTMORTEMS.md) | Others' incidents plus one of Relay's own. **3 entries** `[MEASURED 2026-08-29, grepped]` — Buttondown connection exhaustion, Cloudflare regex backtracking, and Relay's own abandoned `idle in transaction` sessions freezing DDL. Week 0's DoD wanted 2 and Week 1's wanted 3, so both are met. *This row previously read "**1 entry** … Cloudflare still open" and was stale — Cloudflare is Incident 02 and was closed on Stage Day* |

---

## 🚧 Open items across all weeks

Kept here so nothing quietly disappears between weeks.

> **Month 1 close, `2026-09-11`.** The authoritative line-by-line audit is in
> [`daily/WEEK_04_HANDOFF.md`](daily/WEEK_04_HANDOFF.md) — every unticked Week 4 item there carries **one** line:
> `deliberately deferred` **with an owner**, or `slipped` **with what it specifically needs**. The five rows
> below are the ones Month 2 opens on.

| Item | From | Status |
|---|---|---|
| **`P-43` — no exception boundary on the worker/reaper/dispatcher poll, and no supervisor** | Week 4 Din 5 review | 🔴 **Month 2's first item, and deliberately not fixed on Din 6** — it is the *input* to promise #4's `[NO EVIDENCE]` verdict, so fixing it first would have written Month 1's verdict against a system Month 1 did not have. `except Exception` wraps **only** handler execution; the claim poll — where an idle worker spends nearly all its time — is unguarded. A `26.8 s` database outage killed the process outright `[MEASURED-R]`, and `docker-compose.yml` runs only Postgres. **Two decisions, not one edit:** where the boundary goes (per-iteration `try` vs a supervisor that restarts on exit), and whether a crash-loop backoff is needed. **`pool_pre_ping` gets re-priced after this, and that order is load-bearing** — Din 5 rejected the flag on the stated premise that these loops have built-in exception handling, and that premise does not exist |
| **`P-47` — five `docs/` subtrees are gitignored, so the frozen seals, every BRIEF/KEY, all four handoffs, and `CURRENT_WEEK.md` are local-only** | Week 4 Din 6, at commit time | 🔴 **Opened by trying to commit Month 1's close.** `git ls-files docs/` returns **twelve** files; roughly **90** others under `docs/` are in no commit `[MEASURED 2026-09-11]` — including all four handoffs, the twenty-nine BRIEF/KEY pairs, the **twelve `PREDICTIONS_FROZEN.md` files**, both roadmaps, and `DDIA_CH8_LINKS.md`. **The frozen seal is the sharpest case and it is a verification, not a convenience:** the prediction discipline rests on immutability checked by hashing at C0 and again at C6, and that hash is compared against a shell variable **on a file no commit contains** — real, and unauditable by anyone else. **Not a typo but a classification problem:** `docs/daily/` legitimately holds sealed KEY files that must not be published, and the *directory* is a coarser unit than the distinction being enforced, so the rule is guaranteed wrong in one direction. Cheapest fix is to split the pattern (`KEY*` stays ignored, the rest is tracked). **Deliberately not fixed on Din 6** — `.gitignore` decides what becomes public, which is the user's call, not the reviewer's. **Bundle with `P-45` and `P-29`: one retention decision closes all three** |
| **`P-45` — raw-evidence retention, and it now costs verifications rather than convenience** | Week 4 Din 5 review | 🔴 **Cheapest high-value item on the list.** `.gitignore` contains `logs/`, so three headline numbers are `[REPORTED, NOT VERIFIABLE]` (pool timeout `3.0055 s`, receiver contention `2.8133 s` vs `0.4703 s`, `/healthz` `3.1618 s`) and one gate is unverifiable as a direct consequence — the `pool_size=2` override was *configured*, never *observed*. Also: `requirements.txt` pins **nothing**, all eleven dependencies unconstrained. Needs a decision — a curated committed `logs/evidence/` subtree, or the load-bearing lines copied into the day's log **before** cleanup. This is `P-29`'s shape, reaching the point where it breaks a claim |
| **The two-reaper run — `D-29`'s weakest field has a named owner** | Week 4 Din 6 | 🟡 **Month 2.** `D-29` rejects the Postgres advisory lock **on scope, not on measurement**, because the number that would price it — two reapers concurrently, and their double-reclaim count — was never taken, and Din 5's single-reaper `7.9 s` is process-launch latency (`P-43`). Derivable without a new run: the reclaim `UPDATE` re-checks the lease predicate and reads `matched` from `RETURNING`, so a second reaper is already bounded to `rowcount = 0` — **an argument that the problem is small, not evidence about the lock's cost.** The run also prices the one failure mode a lease does not have: a hung-but-alive lock holder, with no expiry |
| **`P-44` — `/slow-hold` unauthenticated and unbounded in `src/main.py`** | Week 4 Din 5 review | 🟡 **Month 2, `deliberately deferred`.** Not removed on Din 6 because that day changed no `src/`, and because `D-28`'s endpoint inventory carries it **by name** — deleting it would delete the decision's subject. It is the amplifier for the exact `/healthz` cascade `D-28` Cost 1 describes, reachable with `?seconds=100000`. Three options, each weakening the Step 3 measurement slightly (separate app factory under test · environment-gated router · hard cap plus `statement_timeout`) — **pick one on the record.** Related: `/db-ping` duplicates `/healthz`; one should go |
| **Every day's `💡` / `🧠` in the user's own words** | Week 2 → Week 4 | 🔴 **Third week carried, and the only item on this list whose owner is not Month 2 but the user.** Week 2's six days and Week 4's six days are reviewer-written and marked *"user must rewrite this in his own words"*. Also owed: five Week 2 answers, and `DDIA_CH8_LINKS.md` lines 10–13 (reviewer-written at Din 3's close — the items are **present**, the confirmation is not). Recorded here rather than in Month 2's plan because the deadline for rewriting a `💡` in your own words passes the moment you can no longer remember not knowing it |
| ~~`POSTMORTEMS.md` entry #2 (Cloudflare)~~ | Week 0 DoD | ✅ **Closed on Stage Day, and this row was stale for the whole of Week 2.** `POSTMORTEMS.md` holds **3** entries — Buttondown, Cloudflare (regex backtracking / WAF CPU), and Relay's own DDL freeze `[MEASURED 2026-08-29, grepped]`. Both DoD counts are met. Recorded rather than silently deleted: an open-items table that carries a closed item is the same defect as one that drops an open one |
| Day 2's exit code `137` where arithmetic predicted `0` | Week 0 Day 2 | **Cause not identified.** Needs `Measure-Command { docker stop ... }` |
| Day 3 Exp D — `pg_stat_activity` count during pool load | Week 0 Day 3 | Never recorded |
| Day 4 Exp B — fast-fail vs slow-fail contrast | Week 0 Day 4 | **Goal never met, cause not identified.** Raw socket ruled out httpx; retry on Linux/WSL |
| Claim query index | Week 1 Din 1 (`P-03`) | To be **measured** in Week 4, not guessed |
| `ACCESS EXCLUSIVE` lock-queue hazard | Week 1 Din 1 (`D-07`) → **Week 2 Din 5/Din 6** | 🟡 **Half closed and now written down.** *Queue forms and a plain `SELECT` blocks the DDL* is `[MEASURED-R]`; *hold duration* is `[INFERRED]`, substitution declared (`LOCK TABLE`, not the migration). Both halves are in `D-06`'s amendment. Remaining half honestly waits for a large table — **owner: Week 4 operations** |
| Age of the abandoned transactions | Week 1 Din 1 (`P-06`) | Upper bound inferred, not measured |
| `idle_in_transaction_session_timeout` decision | Week 1 Din 1 (`P-06`) | Belongs with pool sizing, Week 4 |
| ~~`ACCESS EXCLUSIVE` hazard — measurement **attempted and failed**~~ | Week 1 Din 3 → **Week 2 Din 5** | 🟡 **Half closed, and the half that closed is the one that had failed twice.** Din 5's reviewer probe held `ACCESS SHARE` in an open transaction and then requested `ACCESS EXCLUSIVE` from a second session while a third read the queue **during** the wait: `pg_stat_activity` showed `wait_event_type = Lock` / `wait_event = relation`, `pg_locks` showed `AccessExclusiveLock granted = false` behind `AccessShareLock granted = true`, and `lock_timeout = 9s` ended it in an error rather than a hang `[MEASURED-R]`. **A plain `SELECT` blocks the whole DDL** — that is now measured, not inferred. **Substitution declared:** it was `LOCK TABLE jobs IN ACCESS EXCLUSIVE MODE`, not the migration, so the lock **mode** and the queueing are measured and the **hold duration** is not. That remaining number is the Option A versus Option B difference, it is microseconds at `104` rows, and it honestly waits for a large table |
| ~~C6 shutdown numbers — exit code, elapsed time (mid-job and idle)~~ | Week 1 Din 3 | ✅ **Closed Din 5**, but `[R]`. Exit codes `0`/`0` are the user's. The elapsed times were reported as ranges (*"~5–6 s"*, *"~1–2 s"*) and had to be measured by the reviewer: **idle `1.123 s`**, **mid-job `5.167 s`**, via `SIGBREAK` + `perf_counter`. `P-10`'s idle claim is now `[MEASURED]` — the flag was observed `0.962 s` late against `0.959 s` of remaining sleep. **Re-run once yourself** so the two headline numbers stop being borrowed |
| Handler timeout — mid-job shutdown is unbounded | Week 1 Din 5 (`P-15`) → **Week 2 Din 6** | ⚠️ **Written, not fixed, and the "one decision with the lease" framing was honoured** — `D-22` is a single entry covering lease, heartbeat and the absent timeout. **Not taken**, with a named reason rather than a shrug: a timeout needs an answer to *what `status` does a timed-out handler get?* (retryable → a slow-but-healthy job burns its budget; terminal → a long job is dead-lettered for being slow), and that needs `D-23`'s bound plus Week 3's dedup to exist. **Owner: Week 3/4.** `P-15` stays open |
| Week 0 Day 2's exit code `137` — now a specific hypothesis | Week 1 Din 5 (`P-15`) | `137 = 128 + 9` (SIGKILL). A handler outliving `docker stop`'s 10 s grace produces exactly this. **Not confirmed** — needs `Measure-Command { docker stop ... }` against a known handler duration |
| Completion evidence is a `print`, not a row | Week 1 Din 5 Step 1 → **Week 2 Din 3** | ⚠️ **Still open, and Din 3 turned the cost from a judgement into a blocker.** Proving job 95's `14.783 s` overlap needed **two** endpoints of an interval, and `job_executions` supplies only one: `executed_at` is the **dispatch** instant, committed by `record_execution()` in its own transaction *before* the handler body runs. Handler-end came from **stdout** — and those three `python -u` captures were deleted at day close, so worker A's `rowcount=1` mark now survives only as a quotation in the log. Measured consequence: the day's headline number cannot be recomputed from the database. Price it at `D-22`, and note the cheap version is one nullable `completed_at`, whose cost is a fourth writer to a row the reaper is already racing · **Din 6 verdict: priced in `D-22` Cost 10 and deferred to Week 3, with the dedup work** — the extra writer needs the fencing/idempotency answer to exist first. Owner named, no longer an unowned carry |
| ~~`ORDER BY created_at` has no tiebreak~~ | Week 1 Din 3 | ✅ **Closed Din 4 Step 0** — claim query orders by `(created_at, id)`, and it was load-bearing for the Din 4 timeline reconstruction |
| ~~`signal.SIGBREAK` unregistered / untested~~ | Week 1 Din 3 | ✅ **Fully closed Din 5** `[R]` — `SIGBREAK` delivered, handler ran in 2–3 ms, exit code `0`, no stuck job. A real `Ctrl+C` keypress (`SIGINT`) is still untimed, but every line after the flag is set is identical |
| DDIA Ch 7 second pass (pp. 233–251) | Week 1 Din 1/3/5/6 | **Slipped a fourth time** — now Din 7. Two corrections to earlier framing: (i) Ch 7 does **not** name the dead-versus-slow problem in `P-16`; that is **Ch 8, "The Trouble with Distributed Systems"** — stop looking for it in Ch 7. (ii) Ch 7 **does** name Din 6's variant-1 failure: **lost update**, with compare-and-set as one of the standard remedies, which is `D-06`'s guard. So Din 6 produced the textbook disease for a remedy Relay already shipped |
| Throughput comparison for `D-01` / `D-02` | Week 1 Din 4 (`P-12`) → Din 5 | ⚠️ **`P-12` repaid; the comparison still not made.** Din 5 gave a clean *baseline* for `SKIP LOCKED` — proven overlap, 4/4 split, 0 duplicates, 32.2 s vs 32 s predicted. There is still no like-for-like run of plain `FOR UPDATE`, `SERIALIZABLE`, or advisory locks. **`D-02` must not claim a throughput result**; use Din 4's lock-wait figure (1.25 s into a 6 s lock vs the full 6 s) |
| ~~Option (c) `UPDATE ... WHERE status='pending' RETURNING`~~ | Week 1 Din 6 (`D-02`) | ✅ **Closed Din 6, and the answer is a distinction rather than a verdict: option (c) is not broken — the naive form of it is.** Two live `psql` sessions, session 1 uncommitted. `WHERE id = (subquery)` → session 2 **blocked**, then returned **the same id** with `rowcount = 1`: a silent duplicate claim, and the row's final state is indistinguishable from one correct claim. Adding `AND status='pending'` → `rowcount = 0`, safe. `FOR UPDATE SKIP LOCKED` in the subquery → no blocking, **next** id. So (c)-with-guard is a sound single-statement claim; what it gives up is that the loser of a race gets nothing instead of doing useful work. `D-02`'s Rejected line now says that |
| ~~`EvalPlanQual` protects concurrent writers~~ | Week 1 Din 4 → corrected Din 6 (`P-17`) | ⚠️ **Din 4 learned the wrong lesson from a happy accident.** EPQ rechecks **the predicate you wrote**, not the invariant you meant. `EXPLAIN` `[R]` shows the uncorrelated subquery compiled to an `InitPlan` constant, so the outer qual is `Filter: (id = $0)` with `status` absent — and the recheck therefore passes. Variants 1 and 2 are the same mechanism producing opposite outcomes. Also: DDIA Ch 7 names variant 1 a **lost update** and names compare-and-set as the remedy Relay already uses |
| Step 2 — the five written Week 2 answers | Week 1 Din 5 → … → **Week 2 Din 6** | 🔴 **SLIPPED — not deferred. Ten consecutive carries, and Week 2 is over, so the label is now permanent for this week.** The honest record: Week 1 Din 5 → Din 6 → Din 7 → Stage Day → Week 2 Din 1 → 2 → 3 → 4 → 5 → 6. It was never scheduled into a slot, never blocked on anything, and `CURRENT_WEEK.md`'s own rule (*"Week 2 Din 1 should not start before this exists in his own words"*) was overridden ten times without a decision. **Four of the five are now *measured*, so writing them has become recall of measurements rather than derivation — the exercise's value has already partly decayed, which is the actual cost of the slip.** Owner: **the user, Week 3 Din 1, before Step 0** — and if it slips an eleventh time the honest move is to delete the item and stop pretending it is owed. **The four questions and the original framing, unchanged:** The reaper's predicate using only today's columns and why it fails; the smallest schema addition and the new failure it creates; job 41 vs 63; a lease shorter than the slowest handler. **Deliberately not written for him** — the value is in naming which column is missing and what adding it breaks, and reading that as prose produces recognition rather than recall. `P-16` has the sub-decisions, `D-01` Cost 1–3 has the framing. **Week 2 Din 1 should not start before this exists in his own words** |
| Job 75 is a landmine for Week 2's fixture | Week 1 Din 6 (`M11`, `[R]`) | **New, and it goes off on its own.** Job 75 is `pending` with `type='send_receipt'`, which is **not** in the worker's `REGISTRY`. The next worker start claims it, marks it `failed`, and writes **no** execution row — `pending 4 → 3`, `failed 8 → 9`, and a **fourth** fixture shape appears. Not fixed because the fix is a choice: deleting it breaks the Case A reconciliation, retyping it falsifies the log. **Din 7 Step 0 decides deliberately.** `P-13`'s shape at row level rather than process level |
| Sequence gap at 79 | Week 1 Din 6 (`M10`, `[R]`) | **First gap in `jobs_id_seq`.** A reviewer probe rolled back an `INSERT`, which consumed the value permanently. `jobs` = 78 rows, `max(id) = 78`, `last_value = 79`, so **the next enqueue is id 80.** Reproduces Din 2's M16, so `P-05`'s first half now rests on two measurements. Week 2's reconciliation arithmetic needs to know |
| Conflict path costs a full poll interval | Week 1 Din 6 (`D-02` Cost 5) | `[INFERRED from code]`. In `run_worker()` the `rowcount == 0` branch leaves `claimed_job` as `None`, which falls through to `asyncio.sleep(POLL_INTERVAL_SECONDS)` — so a worker losing a claim race waits **2 s** even when other `pending` rows are free. Step 6 makes `rowcount = 0` a demonstrated outcome, but it has still never been observed **in the worker**. Cheap to change; not changed in Week 1 |
| `CURRENT_WEEK.md` and `CHAT_HANDOFF.md` are untracked | Week 1 Din 6 | `[MEASURED]` `.gitignore` excludes `docs/roadmap/`, `docs/daily/`, `docs/planning/` and `*CHAT_HANDOFF*`. Deliberate and it keeps the repo clean — but **the two documents a cold reader opens first have no history**, while everything they point into does. BRIEFs and KEYs do not need version control; the pointer arguably does. Din 7 Step 8 |
| A check that cannot fail | Week 1 Din 6 (`P-18`) | **Third instance in six days, first where the flaw was in the *design* rather than the reporting.** Din 6's own Case B never inserted a job, so its sequence reading distinguished nothing — and the BRIEF that contains that check opens by restating the rule it broke. **Carry-forward is mechanical, not a maxim:** for every Part C check write the output if the mechanism is present and the output if it is absent, and if the two lines match, rewrite the check |
| Which mechanism made Din 5's 4 µs claims safe | Week 1 Din 5 (`P-14`) → Din 6 | 🟡 **Still `[INFERRED]`, and Din 6 did not settle it** — Step 6 ran in `psql`, not in the worker, so it priced the two halves separately without saying which one fired in Din 5's convoy. This is now the **last `[INFERRED]` in `D-02`**, and it is ~10 minutes of work: re-run the convoy, capture worker stdout, grep for the `rowcount=0` conflict line. **Zero** occurrences alongside a clean split ⇒ `SKIP LOCKED` did the work and the guard is a backstop. **Any** occurrence ⇒ both mechanisms are live. Either answer is publishable; the current state is the only one that is not. Din 7 Step 3 |
| Does the convoy survive unequal handler durations? | Week 1 Din 5 (`P-14`) | **Open.** One run with alternating 8 s / 2 s handlers would answer it and give `SKIP LOCKED` a more honest workload. Week 4 |
| Poll-interval jitter | Week 1 Din 5 (`P-14`) | **New question, not a fix.** First measured evidence that lockstep is real (4 µs, three consecutive rounds). Trade-off: jitter costs latency to reduce contention that `SKIP LOCKED` already handles cheaply. Week 4 |
| Attempt / claim identifier on `job_executions` — **needed earlier than the roadmap assumes, and Din 3 settled the "possibly"** | Week 1 Din 4 (`P-11`) → Din 6 → **Week 2 Din 3** | 🔴 **`P-11` has now expired in measurement, one week ahead of the retry logic it was scheduled against.** `count(*) > 1` is currently true for job ids **`44, 63, 65, 95`** via **three** structurally different mechanisms `[MEASURED-R 2026-08-26]`: job 44 = the **same** `worker_id` (`worker-12940`) dispatching twice `6m54s` apart on 2026-08-17 (Week 1, never noticed); jobs 63/65 = two different workers a **week** apart, Din 2's reclaim executing; job 95 = two different workers **overlapping by `14.783 s`**, a real duplicate. **`attempts = 0` on all four**, so the database distinguishes none of them and the word "duplicate" can only be attached by reading stdout. Din 4 adds retry as a fourth mechanism. The identifier question is no longer "does it need to exist before the reaper" — the reaper shipped on Din 2 and the instrument was already ambiguous on Din 3. **Din 6 verdict: written up in `D-21`'s amendment (five causes, by id) and `D-22` Cost 11 — same answer in both places so they cannot drift. Still not added, and now labelled *overdue* rather than *deferred*. Owner: Week 3, decided with the idempotency key** so the two do not invent competing identities. What it must answer is written down: *given two rows for one `job_id`, same claim or different, and did their intervals overlap?* · original framing below |
| ~~Attempt / claim identifier — original Din 6 framing~~ | Week 1 Din 6 | `count(*) > 1` stops meaning "duplicate" the moment Week 2 retries land. Din 6 adds the first evidence that the ordering may be wrong: Step 6 produced a duplicate claim that **`jobs` cannot record at all** (the row's final state is identical to one correct claim), so `job_executions` is the only place it would have shown — two rows, one `job_id`. The instrument that would catch it expires in the same week that introduces the reaper, i.e. the second writer to `status`. **`D-22`-shaped question: does the identifier need to exist *before* the reaper rather than *with* the retry logic?** |
| FK and `job_id` index on `job_executions` | Week 1 Din 4 (`D-21`) | Deferred on purpose. FK's three branches all conflict with Week 4 retention; index waits for a measurement (`P-03`) |
| Crash window between claim commit and execution-row commit | Week 1 Din 4 (`D-21` Cost 4) | `[INFERRED from code]`, order-of-milliseconds. Reproducing it needs a sleep injected between the two commits |
| `rowcount = 0` branch — **observed, but not in the worker** | Week 1 Din 4 → Din 5 → Din 6 | ⚠️ **Half closed.** Din 6 Step 6 variant 2 produced `rowcount = 0` on demand in `psql` — the **first observation in the project**, after Din 1 called the guard load-bearing and Din 4/Din 5 both recorded the branch as never having fired (Din 5's two workers claimed 4 µs apart without provoking it). The **worker's** `rowcount == 0` branch is still unexecuted code, and Cost 5 above is why that matters |
| Crash window reachable another way | Week 1 Din 5 (`P-16`) | Job **41** is `running` with **no execution row** — the state Din 4 called `[INFERRED]` and millisecond-wide. Reachable by a lock probe, so a reaper cannot dismiss it as improbable |
| Three stuck rows, two different shapes | Week 1 Din 5 (`P-16`) | **Left on purpose** — jobs **41** (no execution row), **63**, **65** (execution rows present) are Week 2's test fixture. `status='running'` is one value covering two situations that differ in whether side effects may have occurred. A reaper keyed on the execution row would silently strand 41 |
| Reaper safety depends on Week 3, not Week 2 | Week 1 Din 5 (`P-16`) | **Roadmap ordering issue worth noticing now.** Resetting job 63 may repeat side effects; nothing before Week 3 prevents that. The interim guarantee is honestly *"recovered, and possibly duplicated"* — write it down rather than discover it |
| Process/connection count before and after worker runs | Week 1 Din 4 (`P-13`) → **Week 2 Din 1, 2, 3** | 🔴 **Not a habit yet — three consecutive Week 2 days failed the *closing* half while passing the opening half.** Din 1: two PIDs left (one interpreter). Din 2: a reaper left `~33 min`. Din 3: **two genuine worker processes left `~4h 55m`** — PIDs `54544`/`42264`, two 2-thread interpreters with **two** asyncpg backends (`1686`, `1689`) whose `state_change` was still advancing at read time `[MEASURED-R]` — while the day's report said *"Active background processes: 0"*. Plus one `psql` session **a day** old (backend `37`, `xact_start NULL`, so hygiene not lock hazard). All three days the arithmetic reconciled anyway, and all three times for the same reason: the leftover process had nothing to move. **That is an empty queue, not detection.** On Din 3 it nearly bit: the reviewer's probe creates a `pending` row, which both live workers would have claimed. **The whole item is one line at close: `Get-Process python`, output into the log** |

---

### Week 2 — new open items

Kept separate so Week 2's own debts do not get lost inside Week 1's list. Every row names what it
**specifically** needs, not its status.

| Item | From | Status |
|---|---|---|
| **`super_slow` exists in no commit** | Week 2 Din 3 (`P-23`) | 🔴 **New, and it is the week's reproducibility hole.** `git log --all -S"super_slow"` returns nothing `[MEASURED-R]`; `HEAD`'s `REGISTRY` is `sleep/boom/slow` and `handle_slow` is still `8.0 s`. Jobs `93`–`96` carry `type='super_slow'` with no handler behind them. **Decision owed, in writing:** bring the long handler back as a **`payload`-driven duration** (no new named handler, and the knob becomes explicit), *or* record in `D-22` that the centrepiece ran on an uncommitted working-tree state and will not re-run. Second option is honest and cheap; leaving it unstated is neither. Dormant hazard: an unknown `type` goes straight to `failed` with no execution row — job 75's exact shape, self-inflicted, currently inert because all four rows are terminal |
| **Heartbeat rejects a *released* lease but not a *re-claimed* one** | Week 2 Din 3 (`P-21`, M8) | **New.** Measured `[MEASURED-R]`: reaper reclaims → heartbeat fires → `rowcount = 0`, row stays `pending`. **Not** measured: reaper reclaims → worker B claims → **worker A's** heartbeat fires. Expected `rowcount = 1`, i.e. the old worker renewing the new worker's lease, after which the reaper can never rescue B. Same generation blindness as worker A's mark returning `1` on B's work. Direct input to **Din 5**'s fencing token — do not build it on Din 4 |
| **Heartbeat interval `10.0 s` is unmeasured** | Week 2 Din 3 (`P-21`) | **Chosen, not measured.** Effective margin is `lease − interval − scheduling delay`, not `lease − interval`, and the write cost is one `UPDATE` per running job per interval on top of the reaper's poll. Din 3 measured what it *achieved* (4 heartbeats, lease never expired) and nothing about whether `10 s` is right. `D-22`, Din 6, and its provenance line must say "chosen on Din 3" |
| **Five of Din 3's eight new job ids have no recorded role** | Week 2 Din 3 (M9) | **New.** `89, 90, 92` (`sleep`) and `93, 94` (`super_slow`) went unnamed; only `91/95/96` were. **`94` matters most**: `super_slow`, one dispatch, no duplicate — if that was a failed centrepiece attempt it is the evidence for Step 3's "move one variable further" iteration, and it is nowhere. The `job_executions` arithmetic still balanced (`+12` against `jobs` `+8`), so this is a rung below `E5`: chain joined, story incomplete. One line per id · **Din 6 verdict: closed as unrecoverable, deliberately.** The rows are terminal, their stdout is gone, and `job_executions` gives one dispatch instant each — so the *role* of `93` and `94` cannot be reconstructed from anything that still exists. `94` (`super_slow`, one dispatch, **no** duplicate) is the one that mattered, and its outcome is now permanently unknown. What survives is the general lesson, and it is already `P-12`'s: **name every id you create in the delta on the day you create it**, because attribution is the one thing arithmetic cannot recover. No owner, because there is nothing left to do |
| **Reaper's stdout capture never reached the log** | Week 2 Din 3 | **New.** Step 1's whole deliverable was a readable reaper output, and the capture was deleted before the reclaim line, the `candidates=0` lines, and the `Claimed job` grep count were copied up. The reviewer had to regenerate the shape from a fresh run to write M2/M6. Standing rule from now: those three items get copied **before** any capture is deleted |
| **Positional indexing into an ORM `RETURNING`** | Week 2 Din 3 (M7) | **New, latent.** `.returning(Job.status, func.clock_timestamp())` compiles to `RETURNING jobs.id, jobs.status, clock_timestamp()` — SQLAlchemy prepends the PK on an ORM-enabled `UPDATE`. `returned_row[0]`/`[1]` are still correct (proved by behaviour: `post_status` printed a status, and `.isoformat()` succeeded on `[1]`), so the `Row` exposes only the two requested columns. Not a bug. But the indices survive on a shape the SQL does not show, and adding or reordering a `RETURNING` column will read the wrong one **silently** — no exception, just a wrong `post_status`. Named access (`returned_row.status`) makes it structural. Do it before the `RETURNING` list changes |
| **Probe job `97` is Din 4's first claimable row** | Week 2 Din 3 (reviewer probe) | **Bookkeeping, and it will distort Din 4 if forgotten.** `type='sleep'`, `pending`, `claimed_at NULL`, `created_at 2026-08-26 15:41:53.408515+00` — the **oldest** `pending` row, so Din 4's first worker claims it before any `boom` job. `+1` execution row, `2 s` of handler. Name it in Din 4's opening baseline or it lands inside the retry delta |
| **Reclaim latency `1.798192 s` — one reading, one schedule** | Week 2 Din 3 (`P-22`) | **Measured once, correctly.** Bounded by `lease + one reaper period`; the pass `226 ms` before expiry correctly matched nothing and the next one caught it `[MEASURED-R]`. Corroborated independently at `≤ 207.098 ms` from Run 1. **What is still unknown:** behaviour with more than one candidate per pass, and under load — the period is `poll + pass duration`, and pass duration grows with candidate count. Week 4, with metrics |
| **The bound is not a row-level bound — `attempts` reached `4`** | Week 2 Din 5 (`P-27`) | 🔴 **New, and it makes one `D-23` sentence false before it is written.** `MAX_ATTEMPTS` is checked in the failure branch, **after** the handler has run; the claim gate consults `status` and `next_attempt_at` and **never** `attempts`. So any row returning to `pending` at the bound — reaper reclaim, or `P-25`'s rejected mark — gets one more **full dispatch including the handler body**. Measured on job `108`: reclaimed at `attempts = 3`, claimed as `attempt=4/3` (the worker prints it), `+1` `job_executions` row, then `dead_letter` at `attempts = 4` `[MEASURED-R]`. **Decision owed on Din 6, in `D-23`:** accept the overdraft and write it, or move `attempts < :max` into the claim gate and own the sweep that then has to terminalise the row (a fifth `status` writer — **not** a Week 2 change) · ✅ **Decided Din 6: the overdraft is accepted and written.** `D-23` now states the bound as *"bounds retry **scheduling**; a row re-entering the queue at the bound costs one more full dispatch, handler body included"*, and the claim-gate alternative is in its `Rejected` list with **Week 3** as the owner if ever taken. The row itself (`108`, `attempts = 4`) is named in the week-close chain rather than absorbed into `dead_letter 3` |
| **Step 7 ran at handler `<` lease, so the shutdown-versus-lease question is untested** | Week 2 Din 5 | 🔴 **New, and it is the one step of the day that did not measure its own subject.** Jobs 106/107 are `type='slow'` with `payload = {}`, so the duration was the default `8.0 s` against a `30 s` lease `[MEASURED-R]` — the lease could not expire, so no reclaim, no second claim, no contested mark. The `10.0 s` heartbeat also never fired (`claimed_at` is `24.7 ms` after dispatch on 107, versus `40.295 s` on Din 3's job 96). **What is needed is now cheap:** one `slow` job with `payload {"seconds": 45}`, `SIGBREAK` at `T = 3 s`. That single run closes this, `P-21`'s untested half, `P-25`'s rejection on the terminal write, and the `[NO EVIDENCE]` hole in `D-22`'s `Cost` · **Din 6: written as a hole, not closed.** `D-22` Cost 8 carries the claim as **`[INFERRED]`** with the reason (*the run happened, its subject did not*) and the run is named in `D-22`'s `Revisit when`. **Owner: Week 3 Din 1, or a named catch-up slot.** Deliberately **not** run on Din 6 — it would have added execution rows to the chain being closed and mixed a measurement day into a writing day |
| **`P-23` is closed in mechanism and open in exercise** | Week 2 Din 3 (`P-23`) → **Din 5** | 🟡 **Progress, and the distinction matters.** `handle_sleep`/`handle_slow` now read `payload.get("seconds", default)` and that is **committed** (`399febb`) — the three-day-old decision finally got written as a yes. But **no row in `jobs` carries a `seconds` key**, so only the default branch has ever run `[MEASURED-R]`, and `super_slow` is still absent from `REGISTRY` while jobs `93`–`96` carry that `type`. All four are terminal, so it is inert — but the unknown-`type` branch writes `failed` **without consulting `attempts`**, which is a second way around the bound |
| **A second no-op `downgrade`, and this one is unavoidable** | Week 2 Din 5 | **New.** `682e01d87be9`'s `downgrade()` is `pass` because Postgres has no way to un-validate a constraint. Running `alembic downgrade -1` from head therefore **succeeds, moves `alembic_version` to `15a05eeb0f79`, and changes nothing** — `convalidated` stays `t` `[MEASURED-R]`. Consequence: version and schema can diverge with no output showing it, and Din 5's *"downgrade failed and rolled back to head"* was actually *two* commands where the first was invisible. **Not fixable; the habit is the fix** — after any `downgrade`, read `alembic_version` **and** `pg_constraint`, not the exit code |
| **`failed` now has exactly one live writer, and 15 historical rows behind it** | Week 2 Din 5 (M8) | **New, and Din 6's chain will trip on it.** The retry writer writes `pending` or `dead_letter` and never `failed`; the only remaining producer is the unknown-`type` branch. So `failed = 15` is entirely historical and spans three different meanings (Week 1 unknown types, Din 4 bounded-outs at `attempts = 3` that would be `dead_letter` today, and job 75). **`dead_letter` does not migrate history** — the week-close count must say which `failed` rows are historical bounded-outs, or Week 3 reads `15` as "jobs that failed once" · ✅ **Done Din 6, and the measurement found *three* shapes where this row predicted two** `[MEASURED 2026-08-29]`: unknown `type` (`8`, `23`, `58` = `does_not_exist`, `75` = `send_receipt`), Week 1 handler failures from before retry existed (`5`, `20` = `sleep`; `6`, `21`, `57` = `boom`), and the six Din 4 bounded-outs at `attempts = 3` (`98`–`103`, all `boom`) which today's code would call `dead_letter`. Written into `D-21`'s amendment and the week-close chain. Nothing renamed |
| **`psql` backend PID `37` has been connected since `2026-08-25 09:55:12+00`** | Week 2 Din 4 → **Din 5** | 🔴 **Now `~3 days`, and it sat through a day that ran `ACCESS EXCLUSIVE` DDL.** `state = idle`, `xact_start NULL`, so zero locks and zero snapshot — `P-06`'s mechanism is **absent**, `P-13`'s hygiene hazard is present. Both existing cleanup checks (`processes = 0`, `idle in transaction = 0`) passed with it there, which is exactly why the BRIEF added a third: `select pid, application_name, backend_start from pg_stat_activity where datname='relay' order by backend_start`. **Not terminated by the reviewer — it is the user's session, and its removal is a named item** · ✅ **Gone as of Din 6, and the way it went is the point.** `pg_stat_activity` for `datname = 'relay'` shows **only** the Din 6 review session (`pid 102`) `[MEASURED 2026-08-29]`, and `relay-db-1` had been up ~35 minutes — i.e. **the container restarted and closed the backend for us.** It was not terminated deliberately, no `pg_terminate_backend` was run, and the habit that would have caught it (the third cleanup check, `backend_start` ordered oldest-first) still has zero successful runs by the user. **Closed by restart, not by discipline** — the check carries into Week 3 |
| Week 1 Din 7 has no log entry | Week 1 Din 7 → carried through Week 2 Din 1–5 | **Open, and it is the user's call because the resolution is a decision.** Din 1's bench reproduced the BENCH block's numbers exactly (`86 / max(id) 87 / seq 87 / 57`), **not** Din 6's (`78 / 78 / 79 / 46`), so Din 7 *ran and was measured* — the provenance half is closed. What ran and what verdict came out is unrecoverable. Either write the entry from whatever survives, or record in `WEEK_01.md` that Din 7 has no entry and stop carrying it as a debt. A second undated gap sits at **`2026-08-24`** (Din 1 = `08-23`, Din 2 = `08-25`); **cause not identified**, and Din 6's week-close should name both · **Din 6 verdict, both named, and both stay open with the same owner — the user, because both resolutions are decisions and not measurements.** (i) **Week 1 Din 7:** its numbers are corroborated (Din 1's bench reproduced the BENCH block exactly, so the day *ran*), but what ran and what verdict came out is unrecoverable. Two honest endings: write the entry from whatever survives, or record in `logs/WEEK_01.md` that Din 7 has no entry and **stop carrying it**. Carried as a debt through eight days now, which is the same anti-pattern as the five written answers. (ii) **`2026-08-24`:** no record, cause still unidentified, and no evidence exists that anything ran. Both go into Week 3's Din 1 with a decision required, not another carry |

### Week 2 Din 6 (close) — what the writing day itself left open

Din 6 changed no `src/` line and produced no new `P-` entry. It produced four documents and three debts.

| Item | From | Status |
|---|---|---|
| **Every `💡 What I Understood` and `🧠 Self-Check` section in Week 2 is reviewer-written** | Week 2 Din 1–5 | 🔴 **The largest carried debt of the week, and it is the one that decides whether the week is retained.** All five days carry a `⚠️ Ye section reviewer ne likha hai` banner, and Din 5's was written at close. The measurements are the user's; the *sentences* are not. **This is not busywork:** the difference between recognising `D-22`'s lease argument and being able to derive it under a viva question is exactly the difference these five sections would close, and `WEEK_02_HANDOFF.md`'s *What Stuck* list is currently a claim with no test behind it. **Owner: the user.** Cheapest honest version — rewrite one day's 💡 per Week 3 day, in his own words, without opening the reviewer's text first |
| **`DDIA_CH8_LINKS.md` lines 10–13 are reviewer-written and unconfirmed** | Week 2 Din 3 → Din 6 | **Carried, fifth day.** Lines 14–18 are the user's own (Din 4/Din 5), three of them with a reviewer correction beneath. Lines 10–13 landed after three consecutive append slips and still need confirming in his own words. The file is on a gitignored path (`docs/ddia_summaries/` is gitignored), so verify by mtime, not by `git status`. **Owner: the user, Week 3** |
| **The third cleanup check has never once been run by the user** | Week 2 Din 4 → Din 5 → Din 6 | **New framing of an old item.** `processes = 0` and `idle in transaction = 0` both passed on the days a three-day-old backend was sitting there; the check that would have seen it (`select pid, application_name, backend_start … order by backend_start`) was added to the BRIEF twice and reported neither time. The backend is now gone **because the container restarted**, not because the check ran `[MEASURED 2026-08-29]`. **Owner: the user — one line at every day close, output into the log** |
| **The week's five per-day deltas reconciled exactly, and that is weaker evidence than it looks** | Week 2 Din 6 | **Recorded, not open.** The chain joined across five status buckets and `job_executions` on the first attempt. What it proves: no row was created or destroyed outside the recorded deltas. What it does **not** prove: that the days were clean — three of the five days reconciled perfectly *while a forgotten process was still polling*, every time because the stray had nothing to move. Keep the sentence, do not upgrade it |

---

### Week 3 — close & register state (2026-09-05)

Week 3 closed with zero `src/` changes and zero net durable database mutations across the checked fingerprint on Din 6. Evidence DB reconciled at `119` jobs, `107` executions, and `9` side effects (`0 + 3 + 2 + 3 + 1 + 0 = 9`).

| Item | From | Status |
|---|---|---|
| **Week 3 Din 5 review score: 6.5 / 10** | Din 5 | Layer A model and mutation verification passed; Layer B test-side witness scored partial (test SQL rather than `src.worker`, concurrent coroutines rather than two worker processes). Slipped to Week 4 as a disposable rerun |
| **Decisions published/audited: `D-24` published, `D-25` audited** | Din 6 | Two-layer idempotency (`D-24`) published; execute dedup (`D-25`) audited; `D-03`, `D-05`, `D-21`, `D-22` amended. **Next free decision: `D-26`** |
| **Problems recorded: `P-28` and `P-29`** | Din 6 | Alembic target ambiguity (`P-28`) and missing raw evidence retention for negative controls (`P-29`) audited and active. **Next free problem: `P-30`** |
| **Overdue debts carried to Week 4** | Din 6 | Per-dispatch claim generation, `completed_at` execution endpoint, transactional outbox build, and 45 s + T=3 s `SIGBREAK` run |
---

### Week 4 — running register (Din 1–3)

| Item | From | Status |
|---|---|---|
| ~~Per-dispatch claim generation~~ | Week 3 debt | ✅ **Closed Din 1** — `jobs.claim_generation` `bigint NOT NULL DEFAULT 0`, nullable historical stamp on `job_executions`, claim/heartbeat/mark all gated on `(status, claim_generation)`. Committed as `d818310` on Din 2's Step 0A `[MEASURED-R]` |
| ~~`completed_at` execution endpoint~~ | Week 1 Din 5 → Week 2 Din 3 → Week 3 | ✅ **Closed Din 2** — `jobs.completed_at timestamptz NULL`, written by `func.now()` inside the **same** gated `UPDATE` as `status`, so no new un-fenced lifecycle writer was created. Backfill is honest: the `122` pre-existing rows are `NULL`, not back-dated `[MEASURED-R]`. **The number it was wanted for is now measurable and the first read is `n = 3`** |
| ~~45 s payload + `SIGBREAK` at `T = 3 s`~~ | `D-22` Cost 8 → slipped twice | ✅ **Closed Din 2, and the answer is worse than the question assumed.** Run 1 (yielding) `succeeded`, mark `rowcount=1`, no reclaim. Run 2 (`block: true`) finished the work, committed its side effect, exited `rc=0`, and the row went back to **`pending`** with `completed_at NULL` — `Conflict on mark`. Both runs' `signal_to_exit` are `~42.2–42.5 s`, so a single shutdown-latency number describes neither `[MEASURED]`. New measured term: `signal_to_handler = 42.068 s` under a blocking handler, because `time.sleep()` on Windows is not interrupted by `SIGBREAK` — so `P-15`'s bound is `handler_duration`, not `remaining_duration` |
| **`claimed_at` cannot be a latency endpoint** | Din 2 | ⚠️ **Named, measured, and written into `DIN_02_DESIGN.md` Faisla 1.** `completed_at - claimed_at = 4.978 s` for a handler that ran `45.036 s`, because `send_heartbeat` overwrites `claimed_at` every `10 s`. The definition's ceiling is `HEARTBEAT_INTERVAL_SECONDS` regardless of handler duration, and after a reclaim it is `NULL` rather than large — so the slowest rows leave the distribution silently. `P-22`, now literal rather than analogous. Chosen definition is `completed_at - created_at`, reported with `n` and grouped by `status` |
| **`completed_at`'s fence is structurally verified, not run-time tested** | Din 2 | 🔴 **`Mark fenced` printed `0` times all day; `Conflict on mark` printed `7`.** Every rejection came from the `status` predicate, because the reaper does not advance `claim_generation` and no third claimant ever landed between a reclaim and a stale mark. **Owner: Din 4 interleaving A**, and its check must be `fence_lines >= 1` — a zero there means "not tested", not "pass" (`P-12`) |
| **Q5's two-worker signalled interleaving** | Week 3 → Din 2 Run 3 → **Din 4** | 🔴 **Slipped a third time, and this time the cause is mechanical, not scheduling.** `harness_sigbreak.py` anchors on `Executing job <specific id>`; two carried `pending` rows sorted ahead of the target under `ORDER BY created_at, id`, the worker claimed `133` while the anchor waited for `134`, and the harness killed the child after `180 s` without ever sending a signal `[MEASURED-R]`. Din 4 must either let the harness enqueue its own job or anchor on `Executing job \d+` with post-hoc id confirmation |
| **The void run measured something the planned run would not have** | Din 2 | ✅ **Recorded rather than re-rolled.** With no signal and two unsupervised workers, job `132` took **`7`** dispatches across **`4`** workers and job `133` took `5` across `2` — and each holds exactly **`1`** `side_effects` row, with `side_effects_id_seq` advancing `+15` for `+3` rows `[MEASURED]`. Dispatch count is unbounded (`P-27`); committed local effect count is `1`. That is Din 1's generation-independent `effect_key` decision paying off under a harder test than the one designed for it |
| **Prediction score `0.0 / 5.0`, and the process cause behind it** | Din 2 | 🔴 All five frozen predictions are `idk`, which scores `0` and **is accurate data**. The load-bearing finding is separate: `DIN_02_ANSWERS.md`'s SHA-256 equals the frozen file's, so `Observed + meri explanation` and `After KEY` were never written. Beat 4 of the daily protocol — *write your own explanation before opening the KEY* — did not happen, and that is the beat the protocol's value sits in. **Owner: the user, before Din 3's Step 0.** The KEY is already open, so filling these costs nothing in integrity and is the only way today's measurements become recall |
| **Three carried `pending` rows** | Din 2 close | 🔴 `132` (`attempts 7`), `133` (`attempts 5`), `134` (`attempts 0`) — all `effect` / `{"seconds": 45, "block": true}`. **Must be drained through the production path with the reaper OFF** (`P-05`); with a live reaper each one re-enters the queue about every `46 s` forever. Owner: Din 3 Step 0. The closing gate that should have caught this is `P-31` |
| **Problems recorded: `P-30`, `P-31`, `P-32`** | Din 2 | `P-30` a rejected mark withholds the diagnosis, not just the transition (`P-25`'s third carrier) · `P-31` the closing bench never asserts a drained queue · `P-32` the lifecycle-grep gate read one of two producers' logs. `P-32`'s code half is **fixed** in `src/reaper.py`; its gate half is open and belongs to Din 3's Part C. **Next free problem: `P-33`** |
| **`DECISIONS.md` untouched on Din 2, deliberately** | Din 2 | Correct per the Week 4 plan: `D-26`'s `Cost` needs the Din 4 real-process witness, and `D-28` needs Din 5's numbers. Two amendments are **queued** for Din 6 rather than written early: `D-22` gains the `handler_duration` vs `remaining_duration` distinction, and `D-26`'s `Cost` gains `P-30`. **Next free decision: `D-26`** |
| **`src/reaper.py` is uncommitted** | Din 2 reviewer fix | 🔴 The `job_id=`/`[reclaim]`/`pre_generation` change is on disk, verified, and **not committed**. Commit it as its own commit in Din 3's Step 0, separately from Din 3's own code — otherwise `git bisect` collapses a log-format change into an outbox feature, which is the mistake Din 1 already made once |
| **`DDIA_CH11_LINKS.md` re-anchor to `D-24`/`D-25`** | Din 2 DOC-SYNC | 🔴 Not done. Carried to Din 3. `DDIA_CH8_LINKS.md` lines 10–13 remain Din 6's |
| ~~Transactional outbox + dispatcher + a receiver with its own idempotency key~~ | roadmap Section 7 → Week 4 Din 3 | ✅ **Closed Din 3, and the atomic half is proved with the right instrument.** `w4d3_outbox` migration, `handle_effect` writes `side_effects` + `outbox` in one `session.begin()`, `src/dispatcher.py` holds `FOR UPDATE SKIP LOCKED` across the HTTP call, `src/sink.py` is an independent receiver with a `SINK_DEDUP` switch. Job `135`: `pair\|1\|1` **and `xmin_same\|true`** — the same PostgreSQL XID committed both rows, which `pair\|1\|1` alone could not establish (two separate transactions also produce `1\|1`). Job `136` with `crash_at: before_commit`: `rows\|0\|0`. Committed as `0129e5b` `[MEASURED]` |
| ~~`crash_at` hook exists in no commit~~ | Week 2 Din 3 (`P-23`) → Din 3 | ✅ **Closed properly, and this is the second-best thing about the day.** `grep 'crash_at\|os\._exit' src/*.py` returned `0` matches at Din 3's start; the hook is now in the tree at two named points (`before_commit` in the handler, `after_http` in the dispatcher), defaults **off**, and is in `git log` rather than a deleted working-tree diff. `P-23`'s whole complaint was that the mechanism producing the week's most expensive measurement survived nowhere |
| ~~`P-32`'s gate half~~ | Din 2 → Din 3 Part C | ✅ **Closed.** C4 loops over the worker **and** dispatcher logs and `throw`s on any line mentioning the job without `job_id=`. The code half was already fixed in `src/reaper.py`. Din 4 extends the same loop to four producers |
| ~~`DDIA_CH11_LINKS.md` re-anchor to `D-24`/`D-25`~~ | Din 2 DOC-SYNC → Din 3 | ✅ **Done** — re-anchor note dated `2026-09-07` present in the file, with `D-24` and `D-25` both referenced `[MEASURED-R]` |
| **The receiver has no `UNIQUE`, so its "exactly once" is timing-dependent** | Din 3 reviewer close (`P-33`) | 🔴 **The day's largest defect, and it is invisible in the day's own transcript.** The BRIEF specified `idempotency_key text NOT NULL UNIQUE`; the shipped table has `NOT NULL` and **no `UNIQUE`** — only `sink_deliveries_pkey` on `id`. Dedup is a constraint-free `SELECT`-then-`INSERT` at `READ COMMITTED`. Three-arm differential, one variable (request overlap): serial `2.1 s` → `applied,duplicate`, **1 row**; concurrent `N=2` → `applied,applied`, **2 rows**; `N=5` → **5 rows** `[MEASURED-R]`. Run A's `1` is correct and is **not** evidence of receiver idempotency — its two deliveries were `2.134 s` apart against a `~4 ms` window. **This contradicts a measurement the project already owns:** Week 3 Din 2 recorded that constraint-free check-then-insert produced `2`, which is exactly why `D-25` chose a named database `UNIQUE` locally. Owner: **Din 4 Step 1**, before Interleaving C |
| **`sink_deliveries` is outside Alembic, and autogenerate wants to drop it** | Din 3 reviewer close (`P-34`) | 🔴 **`alembic check` FAILS on the evidence DB**, proposed operation `('remove_table', 'sink_deliveries')`, because the table is created by `src/sink.py`'s `lifespan` DDL with no model and no revision `[MEASURED-R]`. The next `--autogenerate` writes a revision containing `op.drop_table('sink_deliveries')`, which destroys the `1`-vs-`2` evidence that is Din 3's entire deliverable, and the revision looks routine in review. **Week 2 Din 1's lesson with the sign flipped** — there metadata was behind the DB and the revision was harmlessly empty; here the DB is ahead and the revision is destructive. The tension is real: a model makes the receiver's private storage part of Relay's schema, undoing the boundary Din 3 deliberately built. Four options priced in `P-34`. Owner: **Din 4 Step 1**, alongside `P-28` |
| **The dispatcher has no delay term and no bound, and one bad row starves the backlog** | Din 3 reviewer close (`P-35`) | 🔴 The BRIEF required `D-23`'s numbers (`base 5.0 s`, `×2`, cap `15.0 s`, equal jitter); **no delay term exists.** `asyncio.sleep` sits behind `if not dispatched:` and `dispatched=True` whenever a row was *found*, so the failure path is the only path that never sleeps. Against a closed port: **8 attempts in `~20 s`**, `attempts 1→8`, `dispatched_at` still `NULL`, and `attempts` is **read by no predicate anywhere** — no outbox `MAX_ATTEMPTS`, no outbox DLQ. The `~2.5 s` spacing is TCP connect-failure duration, not backoff (Deletion Test: remove nothing and the pacing is unchanged). Head-of-line blocking: two rows, **7/7 attempts on the failing one, `0` on the deliverable one**, which closed at `attempts=0` `[MEASURED-R]`. So the failure is *"one bad receiver stops the outbox"*, and `undispatched\|0` in a healthy run cannot distinguish that. Owner: **Month 2**; Din 4 records the numbers, Din 6 writes them into `D-27`'s `Cost`. **Do not fix on Din 4** |
| **`outbox.attempts` under-counts the deliveries that happened** | Din 3 reviewer close (`P-35`, second half) | ⚠️ Run A delivered **twice** and closed at `outbox\|137\|1\|1\|1` — `attempts = 1` `[MEASURED-R]`. The increment shares its transaction with the mark, so the crashed attempt rolls back with it: the column counts *committed marks*, not *attempts*, and its name asserts the quantity it cannot observe. `P-11`'s shape on a new table (`job_executions` counts dispatch intent, not execution). Same fix direction in both cases — write the attempt in its own transaction *before* the risky operation — and the same cost, an extra commit on the hot path |
| **Job `136` is a poison pill, and no bound can terminate it** | Din 3 close (`P-36`) | 🔴 `running`, `claimed_at 2026-09-08 11:30:34+00`, `attempts=1`, payload `{"seconds": 1, "crash_at": "before_commit"}`. The Din 3 log's *"will be drained in next reaper cycle"* is **wrong.** `MAX_ATTEMPTS` is evaluated **only** inside `except Exception` in `src/worker.py`, and `os._exit(1)` raises nothing — so `dead_letter` is unreachable at any value of `attempts`. Reaper reclaims on lease expiry without consulting `attempts` and without advancing `claim_generation`; `attempts` increments on claim. Composed: `running → pending → claim → crash → running`, no exit edge, and **2 sequence values burned per iteration** (`seq\|35\|1 → 36\|2` measured), so `P-05`'s gap list becomes unbounded and `P-05` forbids tidying it. Each transition is `[MEASURED]`; **the loop itself was not run** — deliberately, since running it on the evidence DB is the damage. Owner: **Din 4 Step 0**, as Interleaving B′ on a disposable DB. `P-27` past its own edge: an accepted overdraft of exactly one becomes unbounded when the repayment branch is skipped |
| **`P-31` was written as a requirement and satisfied as a table row** | Din 3 close (`P-31` amendment) | 🔴 **Third occurrence of the `P-18` shape, and the sharpest.** `P-31` required that from Din 3 onward every closing bench *assert* `pending = 0 AND running = 0`. Din 3's C5b **table** promises exactly that; the C5b **script** only `throw`s on the frozen hash and the process count, and prints `queue`, `undispatched`, `probe_dbs`. The transcript shows `queue\|0\|1` and the day recorded *"Goal met? YES, completely"* and *"All gates passed … C5b"*. Prose next to a script that does not implement it is not a gate — only the script runs. Din 3 fixed `P-32`'s gate half correctly, so the pattern was understood; the failure is specific to C5b's bench block, where fifteen quantities print and two assert. **Din 4's Part C asserts every quantity in its "Mechanism ghayab" column or labels the row `record only, not asserted`** |
| **Log timestamps measured the pipeline, not the events** | Din 3 close (`P-22` amendment) | ⚠️ Every prefix in `w4d3_runA_sink.log` sits in one `13:16:28.4xx` band while the embedded `echo=True` clock spans `18:24:36`–`18:45:52` local — **~21 minutes of events on one instant** `[MEASURED-R]`. The prefix comes from `ForEach-Object { (Get-Date) }`, which evaluates when the pipeline *reads* the line; PowerShell drains native stdout in chunks. The two lines quoted as Run A's evidence are `1 ms` apart; the real gap is **`2.134 s`** — and that difference is exactly what makes `P-33` invisible. Order and counts are sound; the instants are fiction, and the file mixes UTC (prefix) with local (echo). The existing `snapshot_utc` guard covers the aggregate read time and says nothing about per-line times, so a correct guard sat above an incorrect instrument |
| **`stale_gen_exec` rewrites history and must not be read as an event count** | Din 3 close | ⚠️ It went `17 → 19` and that `+2` is **not** two new stale writes. Today's drain advanced `claim_generation` on jobs `132`/`133`, retroactively relabelling **older** execution rows as stale. Per-job: `128\|3 · 129\|2 · 130\|2 · 132\|7 · 133\|5` `[MEASURED-R]`. It is a `JOIN` result that depends on current generation, not an append-only counter |
| **Prediction score `0.25 / 5.0` — and the beat that was empty on Din 2 was filled** | Din 3 | 🟡 **The score is low and the process improved, and those are two separate facts.** Q3 earned `0.25` (`sink_deliveries = 2` predicted with a reason; the `side_effects` half **not answered**); Q1 was inverted on both sub-answers (predicted one row per table and no sequence advance — actual `0, 0` and `+1, +1`); Q2/Q4/Q5 were `idk`, which scores `0` and **is accurate data**. What changed: all five `### Observed + meri explanation` blocks were filled **before** the KEY, which was the exact failure of Din 2. That is the beat the protocol's value sits in, and it is worth more than the `0.25` |
| **Chain has a one-row blind spot at the boundary id** | Din 3 close | ⚠️ `side_effects` `14 → 19`; the `5` is four new-job rows (`135`, `137`, `138`, `139` — job `136` rolled back) **plus job `134`**, whose effect appears in **no** per-day line because `effect_by_day` filters `job_id > 134` while `carried_drain_day` counts executions. The total reconciles only with this note. Din 4's bench should use `id >= 135` or state the boundary explicitly |
| **`sink_deliveries = 10` does not reconcile to a named list** | Din 3 close | ⚠️ Nine are identified — `job:135`, `job:137`, `job:138`, `job:139` (×2), and three C3 manual keys at `1 + 2 + 2`. The tenth is **not recorded** and will stay that way. `P-29`'s shape: attribution is the one thing arithmetic cannot recover, and the rule is already written — name every row you create on the day you create it |
| **Problems recorded: `P-33`, `P-34`, `P-35`, `P-36` + amendments to `P-22` and `P-31`** | Din 3 reviewer close | Grepped `2026-09-08`. **Next free problem: `P-37`** |
| **`DECISIONS.md` untouched on Din 3, deliberately** | Din 3 | Correct per the Week 4 plan: `D-27`'s `Cost` cannot be written without the Din 4 witness, and `P-35`'s two measured numbers are exactly what that `Cost` needs. `D-26`–`D-29` remain Din 6's. **Next free decision: `D-26`** — `DECISIONS.md` still ends at `D-25`, and `D-09`..`D-20` belong to roadmap Part 2 |
| **A reviewer prediction that was wrong, recorded** | Din 3 reviewer close | `[INFERRED, then refuted]` I expected PowerShell `ForEach-Object -Parallel` with `ThrottleLimit 2` to reproduce `P-33`'s duplicate-row race. It did not — `applied, duplicate`, **1 row**, twice; runspace startup skew is far wider than the `~4 ms` window. The race appeared only from a single `asyncio.gather()`. My first attempt would have concluded *"the receiver is idempotent under concurrency"*, the opposite of the truth — `P-12` again: **a concurrency test that was not concurrent** |
| **`DDIA_CH8_LINKS.md` lines 10–13** | Week 2 Din 3 → … → Din 6 | 🔴 Still unconfirmed in the user's own words. **Third carry.** Owner remains Din 6 |
| **The recorded start commit does not exist** | Din 3 close | ⚠️ The Din 3 log's seal table names `78198f8` as the commit at start; `git cat-file -t 78198f8` returns `fatal: Not a valid object name` `[MEASURED-R]`. The **procedure** was correct — the reaper fix is its own commit (`3145adb`, the latest commit touching `src/reaper.py`) and Din 3 began from `26affde` — only the identifier is wrong. **Cause not identified**; possibly a transcription slip or a hash from a commit that was amended away. No plausible value substituted. New one-command habit: `git cat-file -t <hash>` before writing a hash into a log, because a hash is a claim like any other |
| ~~The fencing token has never rejected a rival claim~~ | Din 1 → Din 2 → **Din 4** | ✅ **Closed, and it is the project's first real fence.** `logs/w4d4_ilA_workerA.log`: `[worker-18308] Mark fenced: job_id=7 held_generation=1 current_generation=2 rowcount=0` — `fenced_lines=1`, `conflict_on_mark_lines=0`, `unstructured=0` across all four producer logs `[MEASURED]`. Din 1 and Din 2 produced seven `Conflict on mark` and zero fences because no third claimant ever landed; what changed on Din 4 was the presence of a real worker B, not the code. The gate was `fenced_lines >= 1` with a `throw`, exactly as `P-12` required, so a zero would have read *"not tested"* rather than *"pass"* |
| ~~Week 3 Din 5's Layer B was test-side SQL, not the production path~~ | Week 3 handoff item 4 → Din 4 | ✅ **Closed.** Five real OS processes on a disposable DB (`relay_w4_witness`), four distinct PIDs, four distinct `worker_id` values in `job_executions`, and interleavings driven only by payload (`seconds: 45`, `crash_at`) and staggered starts — no test-side SQL and no debugger suspension. `job_executions > jobs` strictly, `20 > 9`, which is the precondition that licenses `effects_ok\|0` to mean anything; equal counts would have meant no row was ever dispatched twice and the day's central claim was unobserved rather than proved (`P-12`) |
| ~~The poison-pill loop was reasoned about but never run~~ | Din 3 (`P-36`) → Din 4 Interleaving B′ | ✅ **Closed on a disposable DB, and every clause of the inferred mechanism held.** `4` iterations in `~90 s`; `attempts=4` **and** `claim_generation=4` moving together, which is what distinguishes four real claims from four reclaims (the reaper does not touch the generation); `side_effects=0` committed across four handler runs; `status=running` at `attempts=4 > MAX_ATTEMPTS=3`, the direct observation that the bound's branch is unreachable; `2` sequence values burned per iteration (`side_effects 12→16`, `outbox 4→8`) with `0` rows committed `[MEASURED]`. **Period `~32 s` = `30 s` lease + two `2.0 s` polls — the loop is paced by the lease and the polls wait behind it.** The KEY estimated `~3` and refused to state an exact figure; the extra iteration is phase, not a miss. Job `136` itself remains untouched (`P-05`) |
| **The word "loop" carries no rate, and this is the transferable one** | Din 4 | ✅ Two loops in one codebase, an order of magnitude apart: the poison pill at `~32 s` per iteration because a `30 s` lease gates it, and the dispatcher's retry at `~2.5 s` because nothing gates it and TCP connect-failure supplies the pacing (`P-35`). Same noun, and the difference is which component holds a lease. Any future *"it retries forever"* claim owes a period and the timer that sets it |
| ~~The receiver's "exactly once" is timing-dependent~~ | Din 3 (`P-33`) → Din 4 Step 1 | ✅ **Closed for storage; `narrows`, not `closes`, for delivery.** `UNIQUE (idempotency_key)` added via `w4d4_sink_unique`, and the pre-`SELECT` replaced with one atomic `INSERT … ON CONFLICT (idempotency_key) DO NOTHING` on `rowcount`. Reproduced independently at review from a single `asyncio.gather()`: `N=5` → `1 × 200 applied`, `4 × 200 duplicate`, **`1` row** `[MEASURED-R 2026-09-09]`; `res.rowcount` after `session.commit()` is reliable on this driver stack. **What is unchanged:** five requests still arrived, delivery over the network is still at-least-once, the dispatcher still has no backoff and no bound (`P-35`). The noun that got narrowed is *storage*. The fix carried four costs, each with its own entry below |
| ~~`alembic -c <ini>` is silently overridden by `$env:DATABASE_URL`~~ | Din 4 reviewer close (`P-39`) | ✅ **Closed Din 5 Step 0** — `configure_url()` helper in `alembic/env.py` prioritizes `-c` CLI option over `$env:DATABASE_URL` and announces `alembic resolved_db={db} source={source}`. Verified with 3-arm differential test `[MEASURED]`. Committed `4d5d77a` |
| ~~`sink_deliveries` has two creators and two constraint names~~ | Din 4 reviewer close (`P-38`) | ✅ **Closed Din 5 Step 0** — removed inline `UNIQUE` from `src/sink.py` DDL; Alembic migration `w4d4_sink_unique` is sole owner. Verified with 3-arm differential test `[MEASURED]`. Committed `4d5d77a` |
| **The `UNIQUE` closed the race and destroyed its own negative control** | Din 4 reviewer close (`P-40`) | 🔴 **The cleanest example of the pattern the day should carry forward.** Din 3's `1` meant deduplication *only* because `SINK_DEDUP=0` produced `2` under the same interleaving (`P-18`). The constraint sits below **both** branches of the switch, so `SINK_DEDUP=0` with two concurrent same-key requests now returns `200 applied` + **`500 Internal Server Error`** (unhandled `asyncpg.UniqueViolationError`), storing `1` row `[MEASURED-R]`. It cannot produce `2` on any timing. This is the exact `23505` trace `DIN_04_KEY.md` Q4(b) predicted as the cost of *keeping* the pre-`SELECT` — Faisla 3 removed the pre-`SELECT` and the trace moved to the `else` branch instead of disappearing. **Din 6's Month 1 verdict on promise #3 must read `narrowed`, at the receiver's database, by a named constraint, and must state that the differential proving it was removed the day after it was run.** Din 3's Run A/Run B numbers stay valid as dated history and are not reproducible against current `HEAD`. Habit this earns: **after a fix lands, ask what the fix now makes impossible to observe** — no gate can notice, because a gate only checks that the good arm passes |
| ~~`ON CONFLICT DO NOTHING` waits; `SKIP LOCKED` does not~~ | Din 4 reviewer close (`P-41`) | ✅ **Measured Din 5 Step 2** — holder held lock on `contention-probe` for 3.0s; concurrent `POST /deliver` blocked for **`2.8133s`** on PostgreSQL `transactionid` lock (uncontended baseline 0.4703s, 6.0x slowdown). Proves `ON CONFLICT DO NOTHING` is not `SKIP LOCKED`; conflicting concurrent inserts block on uncommitted transactions `[MEASURED]` |
| **`result=duplicate` asserts the key was seen, not that the payload matches** | Din 4 reviewer close (`P-42`) | ⚠️ Same key with `job_id=333, body={"totally":"different"}` against a stored `job_id=222, body={"holder":false}` → `200 duplicate`, stored row **unchanged**, divergent payload discarded with no error and no line naming the divergence `[MEASURED-R]`. **Discarding is the correct behaviour** — last-writer-wins would be worse — so this is a contract statement, not a bug: the response reads *"I already have this delivery"* and the code knows *"I already have this key"*. That gap is empty only while keys are minted correctly, and `src/dispatcher.py` mints `f"job:{job_id}"` — one key per **job**, not per delivery or per effect. **`D-24` covers enqueue identity and execute identity; delivery identity is a third layer with no written invariant, asserted nowhere and tested nowhere.** Cheap discriminator if wanted: store a body hash and return a named `409 conflicting_payload` on mismatch. Owner: one line in `D-27` on Din 6 |
| **The regression gate cannot fail on anything Week 4 changed** | Din 4 reviewer close (`P-37`) | 🔴 `pytest tests -q → 7 passed in 3.22s`, quoted after every migration as the gate. The suite's **only** `src` import is `from src.models import Job, JobExecution, SideEffect` — no test imports `src.worker`, `src.reaper`, `src.api`, `src.dispatcher`, `src.sink`, or `src.database`, and neither `Outbox` nor `SinkDelivery` is imported `[MEASURED-R]`. **It would pass identically if `src/worker.py` were emptied.** `P-18`'s shape: the tests are good tests and they do pass; the defect is that the gate is *presented* as protecting the day's changes and is structurally incapable of noticing them, while each day repeats `7 passed` as if the number said something about that day. Separately, `tests/__pycache__/conftest…pyc` and `test_api…pyc` exist while `tests/conftest.py` and `tests/test_api.py` do not, and `git ls-files tests` returns only the two `tests/din5/` files — **never tracked**, so that coverage is unrecoverable from this repo. Owner: **not Din 5** (already at `145 min`); one honest README line on Din 6, coverage in Month 2. Din 5 prints it as `record only` or not at all |
| ~~`P-31` was written as a requirement and satisfied as a table row~~ | Din 2 → Din 3 → **Din 4** | ✅ **Implemented as a `throw` and it held.** Din 4's C5 asserted five quantities: `frozen_unchanged=True`, `relay_processes=0`, `job136_untouched=True`, `probe_dbs=0`, and the evidence-DB delta — **with the baseline captured in C0 rather than hardcoded**, so the check cannot pass by matching a stale literal. Re-read independently at review: `relay` identical to Din 3's close on every counter, job `136` at `136\|running\|1\|1`, zero probe DBs `[MEASURED-R]`. First day the closing check throws on the quantity it names. **Kept open** through Din 6's reconcile chain — one day is not a pattern, and the chain is the check that spans all five days. One wording fix: the summary says *"strictly `0`"* where what was asserted is equality of **eight** counters plus the revision; `sink_deliveries` sits deliberately outside the frozen set on a day that changes the receiver's schema |
| **Prediction score `1.5 / 5.0` — corrected down from a self-scored `2.25`** | Din 4 | 🟡 **Two overcredits, one cause.** Q2 `1.0 → 0.5`: the rubric's `1.0` row requires the `SKIP LOCKED` result-set mechanism and the frozen text has the outcome only. Q4 `0.5 → 0.25`: the `0.5` row requires (c), which was `idk`, and the frozen (b) describes the **rejected** pre-`SELECT` shape — under Faisla 3 as built, both callers get `200`. Both drew on `### Observed + meri explanation`, which the rubric's first line excludes by name: *"Sirf `DIN_04_PREDICTIONS_FROZEN.md` ka text grade hota hai."* `Q2 = 1.0` reads as *"I knew this"*; the frozen text shows *"I knew the outcome, not the mechanism"*, and keeping those separable is the only reason the number carries information. **The number that actually improved is not this one:** all five `### Observed` blocks were filled before the KEY **and four are correct on the mechanism** — `SKIP LOCKED` filtering the result set, the reaper not advancing the generation, non-transactional sequences, the two-shape status-code split. Din 2 empty → Din 3 filled → Din 4 filled and right. **Drill before Din 5:** Q3(c)/Q4(c) were both `idk` with the same answer shape (what happens to the values a rejected statement was carrying — `P-25`/`P-30`), and Q5(a) is the one to re-derive cold: given a loop built from a `30 s` lease and two `2.0 s` polls, write the period before looking |
| **`src/database.py` prints `resolved_db=<db>` at import time** | Din 4 | ⚠️ Unconditional, at module import. It is what made the five-process pre-flight possible (`5/5` → `relay_w4_witness`) and it should **stay** — the engine is built at import from a module-level constant, so a wrong target is already a fact before the first query and there is no lazy resolution to catch later. The cost: every process importing `src.database` emits that line first, including `pytest` and any future JSON-on-stdout tool. Din 5's load harness must not parse `src` stdout as structured output without accounting for it |
| ~~Evidence DB is one revision behind head, and `alembic check` is red for a new reason~~ | Din 4 close | ✅ **Closed Din 5 C5** — deduplicated legacy duplicate rows on `sink_deliveries` on `relay`, migrated to head `w4d4_sink_unique`. `alembic check` is green `[MEASURED]` |
| ~~Six files uncommitted at Din 4 close~~ | Din 4 close | ✅ **Closed Din 5 Step 0A** — committed Din 4 feature work separately, followed by Step 0D repair commits. Tree clean |
| **Problems recorded: `P-37`–`P-42` + amendments to `P-28`, `P-31`, `P-33`, `P-36`** | Din 4 reviewer close | Grepped `2026-09-09`. Next free at that point: `P-43` |
| **A migration ran an unguarded `DELETE` on the evidence database, and the delta gate could not see it** | Din 5 reviewer close (`P-46`) | 🔴 `sink_deliveries` went **`10` → `7`** `[MEASURED-R 2026-09-10]`. Commit `cb17c36` added a `DELETE … USING` inside `w4d4_sink_unique`'s `upgrade()` before `create_unique_constraint`, and it ran against `relay`. Surviving ids `1,2,4,6,7,8,9`; gone `3,5,10`; `sink_deliveries_id_seq.last_value` still `10`, so the gaps are permanent and readable. **The `DELETE` was necessary** — a `UNIQUE` cannot be created over duplicate keys — so this is an unrecorded **cost**, not a mistake, and it has three separable properties: **irreversible** (`downgrade()` drops the constraint and cannot restore rows, so C1b arm 3's *"clean round trip"* was clean in **schema**, not in **data**), **unlogged** (no row count printed, so `-3` appears nowhere in the day's transcript), and **unconditional** (it deletes whatever it finds in whatever database it is pointed at — `P-28`/`P-39`'s blast radius arriving through the migration body). **And the gate was structurally blind:** C5's bench asserts eight counters plus the revision and `sink_deliveries` sits deliberately outside that set on days the receiver's schema changes, so *"delta `0`"* is **true on the quantities asserted** while three rows left the evidence database. **Both are true; only one was written.** What was deleted is the expensive part: cross-referencing surviving keys against Din 3's record, the three rows were the **second** copy of each duplicated key — two `SINK_DEDUP=0` manual rows and the second `job:139` `[INFERRED from key survival plus the Din 3 log]`. Those were `P-33`'s original measurement and `P-40`'s negative control. `P-40` already noted the control was no longer *runnable*; it is now no longer *readable* either. **The receiver's dedup fix consumed the evidence for the problem it fixed** — that is `D-27`'s best `Cost` line. **Discriminator worth keeping:** on any table that has seen a `DELETE`, `count(*)` alone lies — `7` reads as *"seven deliveries happened"*; the sequence at `10` is the witness that survives deletion |
| ~~Din 3's tenth `sink_deliveries` row is unrecorded~~ | Din 3 reviewer close → **closed Din 5 reviewer close** | ✅ **There was never an unidentified tenth row.** Din 3's close said *"nine are identified and the tenth is not recorded"*, but the list it gave sums to ten: `job:135` + `job:137` + `job:138` + `job:139`×2 + three manual C3 keys at `1 + 2 + 2` = `1+1+1+2+5 = 10`. The shortfall was an arithmetic slip. **Do not carry this mystery into `WEEK_04_HANDOFF.md`** |
| **Problems recorded: `P-43`, `P-44`, `P-45`, `P-46` + amendments to `P-31`, `P-37`** | Din 5 reviewer close | Grepped `2026-09-10`. **Next free problem: `P-47`.** `P-43` — no exception boundary around the worker's or reaper's DB poll, so a database restart kills the process and Relay ships no supervisor. `P-44` — `/slow-hold` committed into `src/main.py`: unauthenticated, unbounded `seconds`, pins a pooled connection. `P-45` — Steps 2 and 3 have no retained artifact and Faisla 3's `Chosen` script is absent from the tree. `P-46` — a migration's unguarded `DELETE` removed three historical rows from the evidence database. **`DECISIONS.md` untouched on Din 5, deliberately** — `D-26`–`D-29` remain Din 6's, and Din 6's own C0 gate greps `^## D-2[6-9]` and `^## P-4[7-9]` **before** writing, to catch the collision that has already happened twice |
| **`DECISIONS.md` untouched on Din 4, deliberately** | Din 4 | Correct per the Week 4 plan. `D-26`–`D-29` are Din 6's and their subjects are reserved by name (`D-26` fencing · `D-27` outbox · `D-28` observability · `D-29` no leader election). Din 4's four `Faisla` sections live in `DIN_04_DESIGN.md` and feed `D-26`/`D-27`; writing an entry today would take Din 6's largest input from it. Three `Cost` lines are now **queued**: `D-26` gains the measured fence and `P-30`; `D-27` gains `P-40` (the control that the fix removed) and `P-42` (delivery identity has no written invariant). **Next free decision: `D-26`** — `DECISIONS.md` still ends at `D-25`, and `D-09`..`D-20` belong to roadmap Part 2 |
| **A reviewer prediction that was wrong, recorded** | Din 4 reviewer close | `[INFERRED, then refuted]` Reading the `alembic/env.py` diff, I expected `.env` to leak into migrations — `env.py` imports `src.models`, so `load_dotenv()` would run, populate `DATABASE_URL` from `.env`, and point every `-c`-isolated migration at the evidence DB. It does not: `src/models.py` imports nothing from `src.database` `[MEASURED-R]`, so `load_dotenv()` never runs inside Alembic, and a clean-shell `-c <ini>` works correctly (`6` tables in the intended target). Written without checking, it would have read as a severe live defect when the real one is narrower and conditional. Worth keeping because the safety here rests on **one import line in a file nobody edited for this purpose** — a property, not a guard, and `P-39` names the two edits that would undo it |
| **What is not re-verifiable from Din 4, stated rather than filled in** | Din 4 reviewer close | ⚠️ C4's `20 > 9` and the `resolved_db` five-of-five were on `relay_w4_witness`, which was dropped by design. Those claims rest on the day's own transcript (`logs/w4d4_*`), `[MEASURED-R from logs]`, and cannot be reproduced. The evidence DB half **was** independently re-read and is unchanged. No substitute numbers invented |
| **Connection budget arithmetic & attribution** | Din 5 Step 1 | ✅ **Measured Din 5, and the cleanest gate of the week.** `max_connections = 100`, `superuser_reserved_connections = 3` → `97` available; `5` idle connections (`1` per process, `QueuePool` fills lazily); per-engine ceiling `5 + 10 = 15`, fleet ceiling `75`. `application_name` attribution verified independently — all five `logs/w4d5_step1_*.stdout.log` open with distinct non-empty names (`api` · `dispatcher` · `reaper` · `sink` · `worker`) on `relay_w4d5_step1` `[MEASURED-R 2026-09-10]`, which is exactly the trap `DIN_05_KEY.md` warned about (`where application_name = :app` returning `0` for the wrong reason) being avoided. **One correction to the verdict:** the log reads `97 − 75 = 22 comfortable`, which omits the load script's own engine (up to `+15`) and `psql` / `docker exec` sessions (`+2-4`) — the two terms the KEY asked for by name. Full count is `~92-94` against `97`, so headroom is **`~3-5`**. `DIN_05_ANSWERS.md` had this right (`75 + 15 = 90`) and the log went backwards from it. Write **`narrow`**, not `comfortable` |
| **Pool saturation: error name, origin, wait** | Din 5 Step 2 | ⚠️ **`[REPORTED, NOT VERIFIABLE]`** — Worker 2 timed out after `3.0055 s` with `sqlalchemy.exc.TimeoutError: QueuePool limit of size 2 overflow 0 reached`, origin client-side `QueuePool`, invisible to PostgreSQL. The mechanism is sound and nothing contradicts it, but **`logs/w4d5_step2*` does not exist and no probe script was retained**, so the number cannot be re-derived from the repository and C3's *"pool actually was `2` — `2` connections at peak, never `3`, filtered by `application_name`"* cannot be checked at all. `P-45`. Companion `P-41` figure (`2.8133 s` contended vs `0.4703 s` uncontended) carries the same label |
| **`/healthz` cascading outage trap** | Din 5 Step 3 | ⚠️ **`[REPORTED, NOT VERIFIABLE]`** — `/healthz` running `SELECT 1` on the shared pool timed out at `3.1618 s` under saturation, against a `2.0 s` probe timeout. The argument holds on its own terms: a health check that contends with its own subject cannot distinguish *saturated* from *dead*, and the orchestrator's remedy for *dead* is a restart, which kills in-flight work and feeds the load that caused it. But **`logs/w4d5_step3*` does not exist** (`P-45`). **And the harness leaked into production:** `/slow-hold` is now committed in `src/main.py`, unauthenticated, with an **unbounded** `seconds` parameter pinning a pooled connection — the amplifier for the exact failure this step measured (`P-44`) |
| **Sustained load & backpressure curve** | Din 5 Step 4 | ✅ **Measured Din 5, and independently reproducible — the strongest number of the day.** `400` jobs enqueued in `20.16 s` (`19.8`/s). Drain recomputed from the log's own timestamps at review: `186` marks, first claim `15:11:37.299` → last mark `15:12:03.690` = `26.391 s` → **`7.01 jobs/s`**, per-job **`0.1427 s`** `[MEASURED-R 2026-09-10]`; `6.53 jobs/s` if the engine handshake is included, and both numbers are worth keeping because they answer different questions. `186` processed `+` `214` pending `=` `400` closes the arithmetic. `POLL_INTERVAL_SECONDS = 2.0` never entered it — `0` empty-queue polls during the backlog. Four metrics: p50 `10.2849 s`, p99 `18.3357 s`, retry rate `0.0%`, DLQ `0`. **One correction:** the log's `pending` series `91 → 133 → 177 → 221 → 267` implies `+8.8`/s and contradicts its own stated `+12.8`–`13.2`/s; `DIN_05_ANSWERS.md`'s series `91 → 155 → 221 → 267` implies `+12.8`/s and matches `19.8 − 7.0`. **The `ANSWERS` series is the correct one** |
| ~~**Postgres chaos & stale connection recovery**~~ | Din 5 Step 5 → **corrected at Din 5 reviewer close** | 🔴 **The reported conclusion is refuted by the day's own log.** The worker did **not** catch the exception and did **not** survive: `logs/w4d5_step5_worker.stderr.log`'s outermost frames are `worker.py:321 asyncio.run(run_worker())` → `worker.py:185 await session.execute(claim_query)`, so the exception escaped `run_worker()` and **the process exited** `[MEASURED-R 2026-09-10]`. `except Exception` wraps only handler execution; the claim and terminal-mark blocks are unguarded, and an idle worker sits in the unguarded one. Consequently *"next query succeeded cleanly"* has **no evidence** — there was no next query. And only **one** failure was observed, not two: the traceback fails in `_start_transaction` → `_check_open()`, i.e. the **stale-pool** failure; the live-break mid-query never happened. Finally the reaper's `7.9 s` is **not a recovery bound** — its first engine line (`select pg_catalog.version()`) is at `15:22:46.428`, so it was launched **after** recovery and reclaimed on its first poll `63 ms` later; had it been running it would have died with the worker and nothing would have reclaimed the row. `P-43` |
| **`MAX_ATTEMPTS` under a DB outage — still unmeasured, and the mechanism was wrong on both sides** | Din 5 Step 5 → reviewer close | ⚠️ `attempts` is incremented in the **claim `UPDATE`** (`attempts=Job.attempts + 1`), not in `except Exception` — that block only **reads** `current_attempts` to pick `pending` vs `dead_letter`. `DIN_05_KEY.md` located the increment in the wrong place and `DIN_05_ANSWERS.md` inherited it. The answer therefore splits three ways and only the first was exercised: outage **during claim** → transaction rolls back, **`0` attempts spent** (this is what happened — job `2` had already succeeded at `15:22:14`, job `1` was pre-seeded `running` and never claimed by this worker, and the worker was idle-polling at `15:22:16` when the socket died); outage **during handler** → claim's increment is already committed, the mark also fails, the row stays `running`, the reaper reclaims, and the next claim increments again, so **one outage costs two attempts**; outage **after handler success, before mark** → same, plus a duplicate execution. `[MEASURED]` for case 1, `[NOT TESTED]` for cases 2 and 3 |
| **Prediction score `1.0 / 5.0` on Din 5 — corrected down from a self-scored `1.5`** | Din 5 reviewer close | 🟡 **Q1 `1.0 → 0.75`:** (b) and (c) are genuinely complete — the `pool_timeout` bound (`30 s`) and the **application pool** layer both named, and (c)'s arithmetic with the verdict *"kaafi hai"*. **(a) earns nothing:** *"Slow 202 **ya** timeout/error milega"* covers all three options the question offered, which is a hedge and not a prediction, and the mechanism attached to it (*"handler lock lagata hai to koi aur pool isko skip karke doosra job le lega"*) is the **worker claim path**, not `POST /jobs` — `SKIP LOCKED` reasoning pasted onto an `INSERT`. **Q2 `0.5 → 0.25`:** (a)'s number (`5` idle) is right but the mechanism is **absent** — no lazy pool, no *"`pool_size` is retention, not allocation"* — and today's own rule halves an outcome without a mechanism; (b) answers a five-process question with `15`, which is the *"ceiling and observed treated as one"* error the KEY named as the day's most common; (c) `idk`. **Q3/Q4/Q5 `0.0`** stand. **And a process defect larger than the score:** the log's scoring table paraphrases the frozen text **upward** in three places — Q1(c) gains a Little's Law formulation the frozen file does not contain, Q2(b) gains the `75` fleet ceiling the frozen file does not contain, Q3(a) gains a `0.5 jobs/sec` figure the frozen file does not contain. The frozen file itself is intact (hash `07D46BD6…` re-verified); the defect is in the quoting. **Scoring tables quote the frozen answer; they do not summarise it** |
| **Six of nine sub-answers were `idk`, and the pattern has a shape now** | Din 5 reviewer close | ⚠️ Writing `idk` is the correct move over a dressed-up guess and it stays correct. But Q4(a) is answered by **where a `try` sits** in `src/worker.py`, Q4(b) by the pool's checkout lifecycle, and Q5(a) by `pool_size=2` plus `Depends(get_db)` — these are Situation 1 (vocabulary) plus **source reading**, not Situation 2 (an outcome that can only be measured). The lever is not more confidence, it is reading the code path named in the question before answering it. Din 6's questions are deliberately built on **this week's own evidence** rather than library behaviour, so `idk` there reports something different |
| **Gate C5 — clean on its facts, and the gate as written could not be satisfied** | Din 5 C5 → reviewer close | 🟡 Facts independently re-read and all true: `probes=0`, `processes=0`, `frozen_unchanged=True`, eight data counters untouched (`133\|145\|19\|4\|39\|5\|0\|1`), job `136` at `136\|running\|1\|1`, single head `w4d4_sink_unique`, `alembic check` green `[MEASURED-R 2026-09-10]`. **But the gate contradicted itself:** `$benchSql`'s ninth column is `alembic_version`, the first assertion demanded whole-row equality against the C0 capture, and a second assertion demanded that `relay` move from one-revision-behind to head. If the revision moves, the first assertion must throw — both could not pass. It was relaxed to eight columns at run time, which is the right call, and then reported as *"all gates passed"* with no note that the predicate changed. `P-31`'s shape, and the unsatisfiable gate was **reviewer-written**, not introduced by the day's work |
| **Design Decisions `DIN_05_DESIGN.md` — recorded, and three lines need correcting on Din 6** | Din 5 → reviewer close | 🟡 Faisla 1–4 exist and the reasoning shape is right. Three defects: **Faisla 4's premise is false** — it rejects `pool_pre_ping` because workers and reapers *"operate in continuous polling loops with built-in exception handling"*, and no such handling exists, which **inverts** the deletion test (for a process with no retry loop, `pool_pre_ping` replaces the stale connection **inside** the checkout, so the claim query survives; its `Cost` line also understates *"one transient error"* where the real cost is process death). **Faisla 1's `Cost` names the wrong binding constraint** — `echo=True` is `~11 ms` of a measured `0.1427 s` per job (`~8%`); the bound is the `0.1 s` handler. **Faisla 3 names `step4_load_probe.py` as `Chosen` and that file is not in the tree** (`P-45`) |

